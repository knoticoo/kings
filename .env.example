# King's Choice Management App Environment Configuration
# Copy this file to .env and update the values as needed

# ===== UNIVERSAL DATABASE CONFIGURATION =====
# The app automatically detects the best paths, but you can override them:

# Option 1: Set the main data directory (recommended)
# All databases and data will be stored under this directory
# KINGS_CHOICE_DATA_DIR=/path/to/your/data

# Option 2: Set individual paths (advanced)
# KINGS_CHOICE_MAIN_DB_PATH=/path/to/your/main/kings_choice.db
# KINGS_CHOICE_USER_DB_DIR=/path/to/your/user_databases

# ===== AUTOMATIC PATH DETECTION =====
# If no environment variables are set, the app will use:
# 1. XDG_DATA_HOME/kings_choice (Linux standard)
# 2. ~/.local/share/kings_choice (user home)
# 3. {app_directory}/data (fallback)

# ===== DEPLOYMENT EXAMPLES =====
# VPS as root: KINGS_CHOICE_DATA_DIR=/root/kings_choice_data
# VPS as user: KINGS_CHOICE_DATA_DIR=/home/username/kings_choice_data
# Docker: KINGS_CHOICE_DATA_DIR=/app/data
# Development: Leave empty (uses app directory)

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production

# Discord Bot Configuration (optional)
DISCORD_BOT_TOKEN=your-discord-bot-token
DISCORD_CHANNEL_ID=your-discord-channel-id

# Telegram Bot Configuration (optional)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# Default Language
DEFAULT_LANGUAGE=ru

# Development Settings
FLASK_ENV=production
FLASK_DEBUG=False